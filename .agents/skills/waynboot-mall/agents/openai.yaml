interface:
  display_name: "Waynboot Mall"
  short_description: "Repo-specific patterns and workflows for waynboot-mall"
  default_prompt: "Use the waynboot-mall repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true