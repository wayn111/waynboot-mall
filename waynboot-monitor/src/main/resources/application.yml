server:
  port: 89
  shutdown: graceful # 优雅关机
  servlet:
    context-path: /boot-admin
spring:
  security:
    user:
      name: admin
      password: admin

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: ALWAYS
